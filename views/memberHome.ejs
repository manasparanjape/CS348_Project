<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Member</title>
    <link rel="stylesheet" href="css/common.css">
    <link rel="stylesheet" href="css/home.css">
    <link rel="stylesheet" href="css/searchBooks.css">
</head>
<body>
    <button onclick="window.location.href='/logout'">Logout</button>
    <h1>Hi <%= user.Full_name %></h1>
    <% if (message) { %>
        <p><%= message %></p>
    <% } %>
    <% if (books.length === 0) { %>
        <p>You don't currently have any books rented out.</p>
    <% } else { %>
        Here are the books you've rented:
        <table>
            <thead>
                <tr>
                    <th>ISBN</th>
                    <th>Title</th>
                    <th>Author</th>
                    <th>Year</th>
                    <th>Genre</th>
                    <th>Language</th>
                    <th>Due Date</th>
                    <th>Fine</th>
                </tr>
            </thead>
            <tbody>
                <% books.forEach(function(book, idx) { %>
                    <tr>
                        <td><%= book.ISBN %></td>
                        <td><%= book.Title %></td>
                        <td><%= book.Author %></td>
                        <td><%= book.Year %></td>
                        <td><%= book.Genre %></td>
                        <td><%= book.Language %></td>
                        <td><%= book.Due_date %></td>
                        <td><%= book.Fine %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    <% } %>
    <form action="/memberSearch" method="POST" id="searchForm">
        <label for="Title">Title</label>
        <input type="text" id="Title" name="Title">
        <label for="Author">Author</label>
        <input type="text" id="Author" name="Author">
        <label for="Year_min">Year_min</label>
        <input type="number" id="Year_min" name="Year_min" min="0" default="0">
        <label for="Year_max">Year_max</label>
        <input type="number" id="Year_max" name="Year_max" min="0" default="9999">
        <label for="Genre">Genre</label>
        <input type="text" id="Genre" name="Genre">
        <label for="Language">Language</label>
        <input type="text" id="Language" name="Language">
        <br>
        <input type="submit" value="Search Book">
    </form>
    <button onclick="window.location.href='/changePassword'">Change Password</button>
</body>
</html>
